const initialState = {};

const sportsbetting = function (state = initialState, action) {
    switch ( action.type )
    {
        default:
        {
            return state;
        }
    }
};

export default sportsbetting;
